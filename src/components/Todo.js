import React from 'react';
import uuid from 'uuid';
import App from '../containers/App.js';
import style from './TodoList.css';

const Todo = props => <li>Help</li>;

/*const Title = props => <h1>{props.title}</h1>
(...this.state.data) => this.state.data.forEach(this.state.data.text =>  <li> {this.state.data.text} </li>
);
class Todo extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
            data: [{
                id: 1,
                text: 'clean room'
            },]
        };
    }
    render() {
        return (

            <li> ${listItems} </li>
    }
}*/

export default Todo;